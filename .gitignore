# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local
.env.*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/src/tests/__snapshots__
coverage

# Git hooks (keep the directory but ignore custom hooks if needed)
# .git/hooks/pre-commit is tracked and should be committed

# PHP
# Backend files (uncomment if you don't want to commit PHP files)
# /api/